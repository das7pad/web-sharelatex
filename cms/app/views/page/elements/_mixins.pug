mixin contact_btn(content)
	if content.fields.description
		span.contact-btn-description #{content.fields.description}
	a.btn.btn-default(
		href="/contact"
		event-tracking-ga=content.fields.gaCategory || null
		event-tracking-action=content.fields.gaAction || 'Clicked Contact Button'
		event-tracking-label=content.fields.gaLabel || content.fields.contactButtonText
		event-tracking-mb=content.fields.mbCategory || null
		event-segmentation=content.fields.mbData || null
	)
		| #{content.fields.contactButtonText}
		//- to do - safe values for attributes. for ex, do not want " in the value
	
mixin quote(fields)
	if fields.halfWidth
		.col-sm-6
			.row
				if fields.picture
					.col-sm-3
						img(src=fields.picture.fields.file.url class="img-circle pull-right")
				div(class=fields.picture ? "col-sm-9" : "col-sm-12")
					blockquote !{fields.quote}
						if fields.person || fields.position
							footer
								| #{fields.person}
								if fields.person && fields.position
									| , #{fields.position}
								else
									| #{fields.position}
	else
		.col-sm-9.quote-full-width
			blockquote !{fields.quote}
		.col-sm-3.quote-full-width
			if fields.picture
				img(src=fields.picture.fields.file.url class="img-circle pull-left")
			if fields.person
				h5 #{fields.person}
			if fields.position
				.small #{fields.position}
