extends ../../../../app/views/layout

block content
	.content.content-alt
		.container
			.row
				.col-md-6.col-md-offset-3
					.card
						.page-header
							h1 Link Overleaf to IEEE Collabratec™
							.small.text-primary Please confirm that you would like to link your Overleaf account (#{saml_user.Email}) to IEEE Collabratec.

							form(name="signinForm", action="/org/ieee/collabratec/auth/confirm_link", method="POST")
								input(name="_csrf", type="hidden", value=csrfToken)
								.actions
									button.btn-primary.btn(type="submit") Yes, Link My Accounts
							.small.text-primary If you would like to link a different Overleaf account, please #[a("/logout") sign out], sign in with that account, and try linking your accounts again.
