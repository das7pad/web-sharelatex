mixin doc_rows(docs)
	.row
		each doc in docs
			.col-md-4.template-thumbnail
				a(href=doc.path).thumbnail
					img(src=doc.image_url)
					div #{doc.title}
					//- sanitized by api
					div!= doc.author
